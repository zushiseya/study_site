<div class="container px-5 px-sm-0">
  <h2>Post Management</h2>
  <table class="table table-hover table-bordered custom-table">
    <thead>
      <tr>
        <th>タイトル</th>
        <th>説明</th>
        <th>コメント</th>
        <th>いいね</th>
        <th>画像</th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
      <tr onclick="window.location='<%= admin_post_path(post) %>'" style="cursor: pointer;">
        <td data-label="タイトル">
          <%= post.title %>
        </td>
        <td data-label="説明">
          <%= truncate(post.text, length: 100, omission: '... ') %>
        </td>
        <td data-label="コメント" style="white-space: nowrap;">
          <%= "#{post.post_comments.count} 💬" %>
        </td>
        <td data-label="いいね" style="white-space: nowrap;">
          <%= post.favorites.count %> ♥
        </td>
        <td data-label="画像">
          <%= image_tag post.get_image, size: "100x100" %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>